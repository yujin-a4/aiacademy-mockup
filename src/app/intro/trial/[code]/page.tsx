/**
 * /intro/trial/[code] — 소개 페이지의 **체험 세션**이 안에서 돌리는 수업 화면.
 *
 * 하는 일은 한 줄이다: `/lecture/[code]` 의 정본 플레이어를 **그대로** 여기에도 띄운다.
 *
 * 왜 필요한가: 미들웨어가 `/lecture` 를 로그인 뒤로 막는다(`src/middleware.ts`).
 *   소개 페이지는 로그인 안 한 낯선 사람이 보는 자리라, 그대로 iframe 에 넣으면
 *   수업 대신 로그인 화면이 뜬다. `/intro` 아래는 통째로 공개라 여기로 열어 준다.
 *
 * 왜 복사가 아니라 재수출인가: 수업 화면은 **하나여야 한다.** 여기에 따로 짜 두면
 *   정본이 고쳐질 때마다 체험만 옛날 화면으로 남는다. 경로 이름이 같은 `[code]` 라
 *   `useParams().code` 가 그대로 들어와서, 정본 파일을 한 글자도 안 고치고 재수출만 하면 된다.
 *
 * ⚠️ 이 경로는 **공개**다. 정본과 같은 화면이므로, 수업 화면에 계정 정보를 붙이게 되면
 *   그때는 여기서 가릴지 정해야 한다. 지금은 문항·레일 모두 익명 읽기가 열려 있다(실측).
 */
export { default } from '../../../lecture/[code]/page'
