# 결정 대기 큐

**이 파일은 "물어봤는데 아직 답을 못 받은 것"만 모은다.** 대화 속에 흩어져 있으면 매번 다시
설명해야 하고, 실제로 08-25 에 같은 걸 네 번 묻고도 답을 못 받았다. 여기 한 곳에 쌓아두고
하루 한 번 몰아서 답하면 된다.

**쓰는 법**
- Claude: `/memo` 의 **C 갈래(결정이 필요하다)** 가 나오면 여기에 블록을 하나 붙인다. 고르지 않는다.
- 사용자: `**답:**` 줄에 한 줄로 적는다. 그게 곧 지시다.
- 답이 적힌 블록은 처리한 뒤 맨 아래 **닫힌 것**으로 옮긴다(한 줄로 줄여서).

**블록 모양**

```
## (짧은 제목)
- 물은 날 · 출처(메모 N행 / PR #번호)
- 무엇이 걸리나: 한 문장
- 선택지: ① … ② …  ← Claude 안이 있으면 ★ 표시
**답:**
```

---

## 필기도구·형광펜을 언제 켤까
- 08-25 · 메모 21행(`실패`) + 67행 나머지 절반
- 무엇이 걸리나: 필기 지시 턴에서 도구를 **앱이 자동으로 켜줄지**, 학생이 직접 켜게 둘지.
- 선택지: ① 지시 턴에 자동으로 켠다 ② 학생이 직접 켠다 ③ 강사가 말로만 권하고 안 켠다

**답:**

---

## 핵심요약 — 피드백을 문장마다 줄까, 다 풀고 한 번에 줄까
- 08-25 · 메모 **49행** (48·50·51·52·53 은 08-28 에 닫혔다 — 아래 참고)
- 무엇이 걸리나: **콘텐츠 파트가 우리에게 되물은 것이다.** G 열: *"핵심 개념정리는 한 문제
  끝나면 피드백하도록 작성해놓음. 혹시 수정이 필요하다면 전체 다 풀고 멘트 나오는 게 나을지?"*
- 지금 화면은 **전체 다 풀고 한 번에** 다(다 채우면 강사가 1번부터 순서대로 짚는다).
  46행 답("말한 그대로 입력 → 강사가 피드백할 때 채점")과도 그쪽이 맞물린다 — 문장마다
  채점하면 채점 시점이 다시 입력 시점으로 당겨진다.
- 선택지: ① 지금대로 전체 다 풀고 한 번에 ★ ② 한 문장 풀 때마다 바로 피드백

**답:**

---

## 보기가 떠 있을 때 말·글로 답하면 (OX 를 주관식으로 답하는 경우)
- 09-02 · 메모 **88행** (강유진)
- 무엇이 걸리나: OX 를 골라야 하는 자리에서 학생이 주관식으로 답하면 앱이 그걸 **질문**으로
  받는다. 강사가 답해 주고 대본으로 돌아오면서 "자, 다시 볼게요. {물음}" 을 다시 읽어서,
  버튼을 누를 때까지 같은 말이 되풀이된다.
  · 지금도 보기 글자와 겹치면 답으로 받고(`matchTypedChoice`), "모르겠어요"·헛소리도 거른다.
    걸리는 건 **보기에 없는 말로 제대로 답한 경우**다("사람이 상자를 옮기고 있어요").
- 선택지: **콘텐츠 파트가 낸 두 안 그대로다.**
  ① 주관식 답변도 받는다 — 학생 말을 보기와 뜻으로 견줘 가장 가까운 것을 고른 것으로 친다.
     (틀리게 짚을 위험이 있고, 애매하면 결국 되물어야 한다)
  ② 보기가 떠 있는 동안은 텍스트·음성 입력을 아예 닫는다 — 되풀이는 확실히 끊긴다.
     대신 그 자리에서 질문도 못 한다.
  ★ ③ (내 안) 받되 **되풀이는 끊는다** — 보기와 안 맞는 말이 두 번 오면 질문으로 넘기지 말고
     주관식과 똑같이 짚어주고 넘어간다. 지금 헛소리(`isGibberish`)에 쓰는 규칙 그대로다.
**답:**

---

## Part 6·7 해설을 어디에 둘까
- 09-02 · 메모 **91행** (조정연)
- 무엇이 걸리나: Part 5 는 그쪽 지정대로 **문항 아래 해석 한 줄**로 넣었다(PR 아래 참고).
  6·7 은 "별도 탭 논의 필요" 라고 적어 두셔서 손대지 않았다.
  · 6·7 은 화면이 이미 **지문 | 문항** 두 칸이고, 지문이 여럿이면 그 안에 탭이 또 있다.
    해설 탭을 더하면 탭이 두 겹이 된다.
  · 데이터는 있다 — 문장마다 `ko`(직독직해), 보기마다 `why`(정·오답 근거).
- 선택지: ① 지문 칸 아래에 해석을 통째로 깐다(지금 `showKo` 자리가 그대로 산다)
  ② 지문 탭 옆에 '해석' 탭을 하나 더 만든다
  ③ 문항마다 그 문항의 근거 문장 해석만 아래에 붙인다
**답:**

---

# 콘텐츠 파트 답을 기다리는 것

결정이 아니라 **사례·재현 정보가 없어서 못 여는 것**. 여기는 내가 못 고치고, 그쪽이 적어줘야 열린다.

| 행 | 필요한 것 |
|---|---|
| 30 | 음성인식 — **어떤 말**이 안 됐는지 |
| 37 | 문제 뒤 (1)(2)(3) — **어느 화면**인지 (아직 못 찾음) |
| 57 | 브라우저 강제 종료 — **배포본에서도** 나는지 |

> 부탁할 것 하나: 메모를 적을 때 **강사 이름**을 같이 적어달라고 하기. 70행이 이도윤에선
> 재현이 안 되다가 "윤다은"이라는 한 마디에 바로 잡혔다.

---

# 닫힌 것

<!-- 답이 나온 블록을 한 줄로 줄여 여기로 옮긴다. 예:
- 08-25 필기도구 자동 켜기 → ②(학생이 직접) · PR #170
-->
- 08-28 **이도윤에서 앱이 거들까** → **앱은 맞장구도 하지 않는다.** 이 강사 대본은 정오답에
  따른 발화를 직접 갖고 있다(`tutorIfWrong`, 46곳). 앞에 앱이 "잘했어요" 를 얹으면 곧바로
  대본이 "아쉽지만 아니에요" 를 말하는 일이 생겼다 — `scriptWillAck` 이 다음 줄의 `tutor` 만
  보느라 오답 갈래가 나갈 자리를 못 가려서다.
  · 막은 곳: 스캐폴딩 질문 정답(`2360행`) · 필기/표시 턴 정답(`1586행`)
  · **안 막은 곳: 코칭(실전 오답 리뷰).** 그쪽 대본은 32턴 전부 갈래가 없어(`tutorIfWrong` 0개)
    앱까지 입을 다물면 틀린 학생이 아무 반응도 못 받는다.
  · ⚠️ 메모 **6행** 회신(민정 "간단한 피드백은 하고 넘어가도록")과 부딪히는데, 그건 **윤다은
    쪽에서 그대로 산다** — 그 강사 대본에는 갈래가 없어 앱이 유일한 반응원이다.
  · 남은 것: `scriptWillAck` 이 오답일 때 `tutorIfWrong` 을 보게 하는 건 **안 했다**(2안).
    윤다은에도 같은 어긋남이 있다 — 필요하면 그때 손본다.
- 08-28 **메모 48·50·51·52·53행** — 물어볼 게 아니었다. **G·H(보충) 열에 콘텐츠 파트 답이
  이미 적혀 있었다**(46행 때와 똑같은 실수를 할 뻔했다).
  · 48 민정) "이 방식 괜찮은 것 같습니다" → 하지만 **53(08-20)·46(08-25)이 그 뒤에 뒤집었다.**
    막지 않고 그대로 받는 쪽이 최신이다.
  · 50 민정) "굳이 피드백 문장 따로 또 줄 필요는 없고 정해진 정리멘트만" → 이미 그렇게 돈다.
  · 51 민정) "강사 말 얼마 안되니 끝까지 듣고 완료하기" → 건너뛰기 버튼 삭제.
  · 52 → 이미 문장 단위다(08-20 fillFromSpeech).
  · 53 다원) "오답일 때는 빨간색으로 표시되고 정답 나오고 나면, 강사가 피드백" → 46 과 같은 것.
  전부 PR #173 에 담았다. 남은 것은 49행뿐이다(위 블록).
- 08-26 **메모 37행** — 결정이 아니라 그냥 지우면 되는 거였다. Part 5 실전은 한 화면에
  **문장 카드 1 + 문항 카드 1**이고 빈칸도 `______` 하나뿐이라, 문항 뒤 `(1)` 이 가리킬 대상이
  없다. `fromDb.ts` 두 곳에서 `(${i + 1})` 제거. `focusBlank` 의 `part === 5 ? 1` 은 이 경로에서
  아무 일도 안 하는 죽은 코드다(강조는 `___(n)___` 마커에만 걸리고 P5 는 그 마커를 안 쓴다).
